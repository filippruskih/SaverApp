import React from "react";
import Layout from './Layout'
import './App.css';

function Dashboard() {
    return (
        <Layout className={'App ${theme}'}>
        </Layout>
    );
}
export default Dashboard;